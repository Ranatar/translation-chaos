# 🌍 Translation Chaos Generator

Полнофункциональная система для исследования семантического дрейфа через цепочки машинных переводов.

## ✨ Возможности

### Основные
- 🔄 Переводы через цепочки редких языковых пар
- 📊 Детальная аналитика семантического дрейфа
- 🎨 Множественные визуализации (графики, тепловые карты, траектории)
- 🔍 Трекинг трансформаций ключевых слов
- 💾 Экспорт результатов (JSON, Markdown, PNG)

### Игровые режимы
- 🔮 **Предсказатель** - угадайте финальный дрейф
- ⛏️ **Археолог** - найдите критическую точку невозврата
- 🧠 **Обратная инженерия** - восстановите исходный текст
- ⚔️ **Ежедневные челленджи** - соревнуйтесь с собой

### Система прогресса
- 🏆 30+ достижений с наградами
- 📈 Статистика использования
- 🎯 Разблокируемые предустановки

## 🚀 Установка

### Требования
- Node.js 18+ (для ES modules)
- npm или yarn

### Шаги

1. **Клонировать/создать проект**
```bash
mkdir translation-chaos
cd translation-chaos
```

2. **Установить зависимости**
```bash
npm install
```

3. **Создать .env файл**
```bash
cat > .env << EOF
# Hugging Face API (бесплатно 30k запросов/месяц)
HUGGINGFACE_TOKEN=your_token_here

# LibreTranslate URL (опционально, если используете self-hosted)
LIBRETRANSLATE_URL=http://localhost:5000

# Порт сервера
PORT=3000
EOF
```

4. **Создать структуру директорий**
```bash
mkdir -p data
mkdir -p frontend/{css,js,components}
mkdir -p backend/{services,routes,database,utils}
```

5. **Скопировать все файлы из примеров выше**

6. **Запустить сервер**
```bash
npm start
# или для разработки с hot reload:
npm run dev
```

7. **Открыть в браузере**
```
http://localhost:3000
```

## 🔧 Настройка

### Получение Hugging Face токена

1. Зарегистрируйтесь на https://huggingface.co
2. Перейдите в Settings → Access Tokens
3. Создайте новый токен (read access достаточно)
4. Скопируйте в `.env`

### Self-hosted LibreTranslate (опционально)

Для лучшей стабильности и контроля:
```bash
docker run -ti --rm -p 5000:5000 libretranslate/libretranslate
```

Или установить локально:
```bash
pip install libretranslate
libretranslate
```

## 📊 Архитектура
```
Backend (Node.js + Express)
├── Translation Service (мульти-движок с fallback)
├── Embeddings Service (HuggingFace API)
├── Analytics Service (детальный анализ шагов)
├── Game Modes Service (логика игровых режимов)
├── Achievements Service (система прогресса)
└── SQLite Database (локальное хранилище)

Frontend (Vanilla JS + Chart.js)
├── Main App (управление состоянием)
├── Visualizations (графики, карты, траектории)
├── Game Modes UI (интерфейсы игр)
├── Achievements UI (прогресс и разблокировки)
└── Animations (визуальные эффекты)
```

## 🎮 Использование

### Базовый режим

1. Выберите предустановку или создайте цепочку
2. Введите текст
3. Нажмите "Запустить хаос!"
4. Исследуйте результаты

### Игровые режимы

#### Предсказатель
1. Выберите режим "Предсказатель"
2. Оцените цепочку языков
3. Сделайте прогноз дрейфа
4. Получите очки за точность

#### Археолог
1. Запустите обычный перевод
2. В истории выберите "Играть"
3. Выберите шаг с критическим сдвигом
4. Проверьте правильность

#### Обратная инженерия
1. Запустите обычный перевод
2. Попробуйте угадать исходный текст
3. Используйте подсказки (за штраф)
4. Получите оценку сходства

## 🏆 Достижения

Система включает 30+ достижений:
- Базовые (первый запуск, 100 запусков)
- Дрейф (экстремальный, минимальный)
- Игровые (мастер предсказаний, археолог)
- Исследовательские (все континенты, полиглот)
- Специальные (случайное возвращение, языковая петля)

## 📈 Оптимизация

### Для production

1. **Кэширование**
```javascript
// Добавить Redis
import Redis from 'ioredis';
const redis = new Redis();
```

2. **Rate limiting**
```javascript
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100
});
app.use('/api', limiter);
```

3. **Мониторинг**
```javascript
import morgan from 'morgan';
app.use(morgan('combined'));
```

4. **Процесс-менеджер**
```bash
npm install -g pm2
pm2 start backend/server.js --name translation-chaos
```

### Производительность

- Кэш переводов: ~95% hit rate
- Embeddings кэш в БД
- Batch запросы к HuggingFace
- Connection pooling для БД

## 🐛 Troubleshooting

### Проблема: "All translation services failed"
**Решение**: Проверьте доступность API, используйте VPN, добавьте больше сервисов

### Проблема: "Embeddings timeout"
**Решение**: Увеличьте timeout, используйте fallback на Jaccard similarity

### Проблема: "Database locked"
**Решение**: WAL режим уже включён, проверьте concurrent writes

## 🔮 Roadmap

- [ ] WebSocket для real-time обновлений
- [ ] Multi-user поддержка
- [ ] Leaderboards
- [ ] Больше языковых движков
- [ ] Local embeddings (Transformers.js)
- [ ] Голосовой ввод/вывод
- [ ] API для внешнего использования

## 📄 Лицензия

MIT

## 🤝 Contributing

Pull requests welcome!

## 📧 Контакты

[Your contact info]